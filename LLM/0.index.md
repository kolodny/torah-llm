# LLM Index — project state (finger on the pulse)

This is the canonical, always-current index of the `LLM/` work log. It tracks every
`NNN.name.md` item and summarizes **where the project stands right now**. Update it whenever an
item is added or state changes. (Sorts first in the folder, before `001`…)

## What this project is

Multi-source Torah platform that runs **entirely in the browser**. A **canonical**,
source-independent catalog (books keyed by title) is split into small per-book SQLite files; the
app boots from a tiny TOC DB and downloads each book's chunk on demand into persistent browser
storage (SQLite-WASM over the OPFS SAH-pool VFS) — no giant DB. Each book carries many **editions**
(source × language × version), compared side by side.

## Current state (2026-06-09)

**Working, verified in-browser (Playwright):**
- Pipeline `npm run data` = fetch each **pinned** source (see `sources.lock.json`) → build
  `data/master.sqlite` → slice into `public/db/` (`db.sqlite` boot TOC + `toc_<id>.sqlite` per book).
  Ingestion lives in **`ingest/`** (per-source adapters: Sefaria JSON, Orayta TXT, Open Siddur TXT). **Jewish-origin
  sources only** — OpenScriptures morphhb/WLC was removed (Christian-affiliated project); same rule
  excludes STEPBible, OpenScriptures HebrewLexicon, tanach.us/UXLC.
- Content: **Torah** (Genesis–Deuteronomy) + **Rashi** commentary. Editions incl. Sefaria he/en, JPS
  1917, French, Orayta — and **Orayta's Rashi overlays Sefaria's at comment level** (verified
  row-for-row). Genesis = 5 editions; Rashi on Genesis = 3.
- **Liturgy** (Open Siddur, public domain): **Seder ha-Tefillot (Rambam)** [he] + **Standard Prayer
  Book (Singer)** [en], under the canonical `Liturgy` category. See [017](017.jewish-sources-and-liturgy.md).
- Browser app (`npm run dev`): catalog tree, on-demand book download, **bidirectional commentary
  links** as real anchors (cmd-click → new tab), **URL state** via react-router (incl. selected
  editions as `ed=` params), scroll-to + highlight the focused ref.
- **Editions reader:** side-by-side columns; chips are **reorderable** (dnd-kit) and drive column
  order, **persisted to the URL**; hovering a chip shows provenance (`info`). Default: **English
  left, Hebrew right.**
- **Canonical catalog:** books keyed by title, categories by path — adding a book never renumbers
  others; overlapping books dedupe into one book with selectable editions. See
  [013](013.editions.md) (canonical/editions model — supersedes 012's namespaced ids).
- **Source pinning:** `sources.lock.json` records each source's git SHA / version → reproducible
  clone without committing corpora (`/data`, `/public/db`, `*.sqlite` gitignored → small clones).
  Not submodules (would vendor whole repos; can't represent Sefaria's GCS bucket). See [016](016.housekeeping.md).
- **Cross-tab sharing**: leader-tab owns the SAH-pool worker; a SharedWorker brokers ports so all
  tabs share one DB; Web Locks handles leader handoff. No COOP/COEP headers.
- **Self-healing boot DB** via `PRAGMA user_version` / `BOOT_VERSION` (canonical value lives in
  `export const BOOT_VERSION`, `shared/schema.ts` — bumped whenever cached DBs must refresh).

**Next:** Orayta Ramban/Ibn Ezra/Sforno on the Torah (net-new commentaries, same parser — see
[015](015.orayta-commentary.md)); **Jewish-origin** sources from [014](014.data-sources.md) — Dicta
(vocalized Talmud, Library, Nakdan), more Open Siddur (other siddurim / live API), PocketTorah audio;
a full import at scale; Sefaria cross-reference link CSVs once the corpus has more commentaries.
(Jewish-only source policy: [017](017.jewish-sources-and-liturgy.md).)

**If the app won't load** (stale browser state from this session's worker rewrites): close all
tabs of localhost:5173 and reload, or DevTools → Application → Clear storage, or "Wipe local DB".

## Items

| # | Item | Summary |
|---|------|---------|
| 001 | [Investigation](001.investigation.md) | Read the abandoned reference; mapped architecture & the breakage. |
| 002 | [Design decisions](002.design-decisions.md) | Stable string keys, Torah subset, minimal stack, raw SQL. |
| 003 | [Sefaria data source](003.sefaria-data-source.md) | Sefaria moved data git → public GCS bucket; fetch subset over HTTPS. |
| 004 | [Ground-up rebuild](004.ground-up-rebuild.md) | Fresh file layout and rebuild steps. |
| 005 | [Data pipeline](005.data-pipeline.md) | fetch → build → slice; stable string keys; verified output. |
| 006 | [Browser app](006.browser-app.md) | SQLite-WASM worker + comlink client + React UI; clean prod build. |
| 007 | [Verification](007.verification.md) | Playwright: on-demand load + OPFS persistence proven. |
| 008 | [Commentary & links](008.commentary-and-links.md) | Rashi commentary + derived links; per-verse on-demand commentary. |
| 009 | [Navigation, URL state, multi-tab](009.navigation-url-multitab.md) | Bidirectional links, react-router URL state, per-tab pools (superseded by 011). |
| 010 | [Follow-ups & open decisions](010.followups.md) | Cross-tab sharing options (resolved → 011); **add more sources** roadmap (§B, active). |
| 011 | [Cross-tab sharing](011.cross-tab-sharing.md) | Leader-tab + SharedWorker port broker → tabs share one DB; Web Locks handoff. Verified. |
| 012 | [Multi-source](012.multi-source.md) | Source dimension + per-source adapters; added Open Scriptures (WLC), Orayta. |
| 013 | [Editions](013.editions.md) | Dedupe overlapping books → one canonical book; sources + Sefaria versions as selectable editions; data/ consolidation. |
| 014 | [Data sources](014.data-sources.md) | Ranked, license-verified survey of open sources to ingest next (STEPBible, Dicta, HebrewLexicon, tanach.us…). |
| 015 | [Orayta commentary](015.orayta-commentary.md) | Orayta's refs live in per-book `.conf` `AddSource`; ingested Rashi-on-Torah as a 3rd edition, comment-aligned with Sefaria. |
| 016 | [Housekeeping](016.housekeeping.md) | Renamed scripts→ingest; `sources.lock.json` pinning + small-clone gitignore; edition `info` tooltips; he-right/en-left default; minimalist README. |
| 017 | [Jewish-only sources + liturgy](017.jewish-sources-and-liturgy.md) | Policy: Jewish-origin sources only (removed morphhb/WLC; excluded STEPBible/HebrewLexicon/tanach.us). Added Open Siddur liturgy (Rambam + Singer) under `Liturgy`. |

## Convention

Append a new `NNN.short-name.md` per workload/decision/milestone; keep each self-contained; then
update this index's table + "Current state". `0.index.md` is the dashboard, never a content item.
