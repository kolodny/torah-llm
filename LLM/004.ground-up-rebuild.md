# 004 — Ground-up rebuild

**Date:** 2026-06-09

Per the project owner, this is a **clean rebuild**, not a copy of the abandoned reference.
The earlier rsync-copy was wiped; only this `LLM/` log was kept and re-homed here.

## Target file layout

```
package.json            # minimal deps (no MUI/drizzle/router)
tsconfig.json
vite.config.ts          # base '/', exclude sqlite-wasm from optimizeDeps, ES workers
index.html
.gitignore              # ignore sefaria/, db/, public/db/, node_modules, dist
cspell.json             # project words (Sefaria, Tanakh, OPFS, …) to quiet the linter
README.md
LLM/                    # this indexed work log

scripts/
  sefaria.ts            # shared: paths, schema cache, ref-path flattening (createRefPath/format)
  fetch-subset.ts       # download Torah subset from GCS → ./sefaria/
  build-master.ts       # ./sefaria flat files → db/master.sqlite
  slice-db.ts           # db/master.sqlite → public/db/db.sqlite (toc) + toc_<id>.sqlite (per book)
  schema.sql.ts         # SCHEMA_SQL string shared by build + slice

src/
  main.tsx              # React entry
  App.tsx               # UI: TOC tree + per-book download + content viewer (he/en)
  app.css
  db/
    schema.ts           # row TS types (+ re-export SCHEMA_SQL)
    worker.ts           # SQLite-WASM worker: open/exec/upload/merge over OPFS SAH pool
    client.ts           # browser glue: spawn worker, comlink wrap, typed query helpers
    slice-path.ts       # sliceFileName(id) shared by slicer + client
```

## Steps & status

1. [x] Wipe copy; author minimal `package.json`; clean `npm install`.
2. [x] Fetch Torah subset from GCS into `./sefaria/` (`scripts/fetch-subset.ts`).
3. [x] Write ingestion (`build-master.ts`) with stable string keys.
4. [x] Write slicer (`slice-db.ts`) — keep only content-bearing nodes + ancestors, safe filenames.
5. [x] Write browser worker + client + minimal React UI.
6. [x] Run `fetch → build → slice`; verify DB contents. → see [005](005.data-pipeline.md).
7. [x] `npm run dev` + Playwright MCP: load TOC, download a book, view Hebrew/English.
       → see [006](006.browser-app.md), [007](007.verification.md).

All steps complete for iteration 1.
