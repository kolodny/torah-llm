# 007 — Browser verification (Playwright MCP)

**Date:** 2026-06-09 · dev server `http://localhost:5173/`

Verified the running app with the Playwright MCP browser. All checks passed.

## Results

1. **Boot.** Page loads; `SQLite 3.49.0` reported (WASM + SAH-pool VFS initialized). Catalog
   renders the pruned tree: Tanakh → Torah → Genesis/Exodus/Leviticus/Numbers/Deuteronomy with
   Hebrew titles and slice sizes (Genesis ↓ 856 KB, …). Network: only `db/db.sqlite [200]`.

2. **On-demand download.** Clicked **Genesis** → network fetched `db/toc_Genesis.sqlite [200]`
   (and nothing else) → button flips to "✓ local" → viewer shows **1533 verses · 50 chapters**,
   1533 verse rows, Hebrew with nikud + cantillation (`בְּרֵאשִׁ֖ית בָּרָ֣א…`) beside English.

3. **Persistence (the core thesis).** Reloaded the page. Genesis is **immediately "✓ local"**
   with no click. Network on fresh load shows the sqlite-wasm library files only — **no**
   `db.sqlite` and **no** `toc_Genesis.sqlite` re-fetch. Chunks persist in OPFS.

4. **Accumulation + any-title keys.** Clicked **Exodus** → only `db/toc_Exodus.sqlite [200]`
   fetched (Genesis not re-fetched) → 1210 verses · 40 chapters. Both Genesis and Exodus now
   "✓ local"; the rest still downloadable. Confirms string-keyed slice paths work per-title and
   local content accumulates.

5. **Clean console.** After adding a favicon and suppressing the benign synchronous-OPFS
   warning: **0 errors, 0 warnings.**

## Conclusion

The thesis is demonstrated end-to-end: boot from a tiny TOC DB, fetch per-book SQLite chunks
on demand, store them locally (OPFS) so reloads need no network, all in-browser. Ready to
expand the corpus (more titles in `fetch-subset.ts`) and add links.
